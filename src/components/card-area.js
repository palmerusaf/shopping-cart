import ProductCard from "./product-card.js";

export default function CardArea() {
  const mapDataToCards = (data) => {
    return <p>coming soon</p>;
  };
  return <div className="card-area">{mapDataToCards()}</div>;
}
